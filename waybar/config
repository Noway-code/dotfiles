{
    "layer": "top",
    "position": "top",
    "modules-left": ["sway/workspaces","clock"],
    "modules-center": ["custom/music"],
    "modules-right": ["pulseaudio", "cpu", "memory", "battery","network"],

     "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-muted": "Muted ﱝ",
        "format-icons": ["", "", ""],
        "on-click": "pavucontrol"
    },

    "custom/music": {
        "format": "  {}",
        "escape": true,
        "interval": 5,
        "tooltip": false,
        "exec": "playerctl --player=spotify metadata --format='{{artist}} - {{ title }}'",
        "on-click": "playerctl --player=spotify play-pause",
        "max-length": 50
    },

    "clock": {
        "format": "{:%Y-%m-%d %I:%M}"
    },


    "battery": {
      "format": "{capacity}% {icon}",
      "format-charging": "{capacity}%  ",
      "format-icons": ["", "", "", "", ""]
    },

    "cpu": {
        "format": "CPU: {usage}%",
        "interval": 2
    },

    "memory": {
        "format": "RAM: {used} MiB",
        "interval": 2
    },

    "network": {
        "interface": "enp0s31f6",
        "format-ethernet": "Ethernet ඞ",
        "format-wifi": "{ifname}   {signalStrength}%",
        "format-disconnected": "Disconnected :(",
        "interval": 10
    }

}
